set $super Mod4
set $alt Mod1
floating_modifier $super

font pango: JetBrains Mono 10

# Keybindings
bindsym $super+Shift+r restart
bindsym $super+Shift+e reload
bindsym $super+Return exec i3-sensible-terminal
bindsym $super+Shift+d exec i3-dmenu-desktop --dmenu="dmenu -i -fn 'MesloLGS NF:size=10'"
bindsym $super+d exec rofi -show drun
bindsym $alt+Tab exec rofi -show window
bindsym Print exec $HOME/bin/scrotselect
bindsym $super+Print exec $HOME/bin/scrotselect -s
bindsym $super+l exec betterlockscreen -l
bindsym $super+Shift+w exec firefox
bindsym $super+Shift+f exec pcmanfm
bindsym $super+Shift+c exec code

# Volume control
set $volumepath $HOME/scripts/i3-volume/volume
set $volumestep 5
bindsym XF86AudioRaiseVolume exec $volumepath -lnpy -i $volumestep
bindsym XF86AudioLowerVolume exec $volumepath -lnpy -d $volumestep
bindsym XF86AudioMute        exec $volumepath -lmny

# Screen brightness control
bindsym XF86MonBrightnessUp exec xbacklight -inc 10
bindsym XF86MonBrightnessDown exec xbacklight -dec 10

# Window control
bindsym $super+q kill
bindsym $super+Left focus left
bindsym $super+Down focus down
bindsym $super+Up focus up
bindsym $super+Right focus right
bindsym $super+Shift+Left move left
bindsym $super+Shift+Down move down
bindsym $super+Shift+Up move up
bindsym $super+Shift+Right move right

bindsym $super+h split h
bindsym $super+v split v
bindsym $super+f fullscreen toggle
bindsym $super+s layout toggle split
bindsym $super+space floating toggle
bindsym $super+Shift+space focus mode_toggle

# Scratchpad control
bindsym $alt+Control+Up exec $HOME/bin/i3-scratchpad
bindsym $alt+Control+Down exec $HOME/bin/i3-scratchpad

# Workspace control
bindsym $alt+Control+Right workspace next
bindsym $alt+Control+Left workspace prev
bindsym $super+1 workspace 1
bindsym $super+2 workspace 2
bindsym $super+3 workspace 3
bindsym $super+4 workspace 4
bindsym $super+5 workspace 5
bindsym $super+6 workspace 6
bindsym $super+7 workspace 7
bindsym $super+8 workspace 8

bindsym $super+Shift+1 move container to workspace 1
bindsym $super+Shift+2 move container to workspace 2
bindsym $super+Shift+3 move container to workspace 3
bindsym $super+Shift+4 move container to workspace 4
bindsym $super+Shift+5 move container to workspace 5
bindsym $super+Shift+6 move container to workspace 6
bindsym $super+Shift+7 move container to workspace 7
bindsym $super+Shift+8 move container to workspace 8

bindsym $super+p move workspace to output right
bindsym $super+o move workspace to output left

mode "resize" {
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Down resize grow height 5 px or 5 ppt
        bindsym Up resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt
        bindsym Return mode "default"
}
bindsym $super+r mode "resize"

# Exit control
mode "exit: [l]ogout, [r]eboot, [s]hutdown [esc]cancel" {
  bindsym l exec i3-msg exit
  bindsym r exec reboot
  bindsym s exec poweroff
  bindsym Escape mode "default"
  bindsym Return mode "default"
}

bindsym $super+Shift+q mode "exit: [l]ogout, [r]eboot, [s]hutdown [esc]cancel"

# Window rules (find class with xprop)
for_window [class=".*"] border pixel 0
assign [title=Firefox] 2
assign [class=Pcmanfm|File-roller|VSCodium] 3
assign [class=qpdfview] 4
assign [class=Telegram] 5
assign [Class=MellowPlayer] 6
assign [class=Lxappearance|Pavucontrol] 7
for_window [title=Zoom] move to workspace 7
for_window [class=Gpicview|feh|File-roller|Lxappearance|Pavucontrol|Zoom] floating enable
for_window [class=URxvt|Firefox|File-roller|Lxappearance|Pavucontrol] focus

# Gap options
smart_gaps inverse_outer
gaps inner 20

# Set colors from Xresources
# Change 'color7' and 'color2' to whatever colors you want i3 to use 
# from the generated scheme.
# NOTE: The '#f0f0f0' in the lines below is the color i3 will use if
# it fails to get colors from Xresources.
set_from_resource $fg i3wm.color7 #f0f0f0
set_from_resource $bg i3wm.color2 #f0f0f0

# class                 border  backgr. text indicator child_border
client.focused          #bf616a $bg     $fg  #bf616a   $bg
client.focused_inactive $bg     $bg     $fg  $bg       $bg
client.unfocused        $bg     $bg     $fg  $bg       $bg
client.urgent           $bg     $bg     $fg  $bg       $bg
client.placeholder      $bg     $bg     $fg  $bg       $bg

client.background       $bg

# Autostart
exec --no-startup-id xsettingsd &
exec --no-startup-id picom --daemon --experimental-backends
exec --no-startup-id xrandr --output HDMI-1-1 --auto --right-of eDP1
exec --no-startup-id betterlockscreen -w
exec --no-startup-id nm-applet
exec --no-startup-id $HOME/bin/load_layout
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec_always $HOME/scripts/i3-alt-layout/alternating_layouts.py

